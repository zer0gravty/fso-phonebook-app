{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "ui:deploy": "git push heroku master",
    "ui:build": "cd ./clientApp && rm -rf ./build/ && npm run build --prod && rm -rf ../build/ && cp -r build ../",
    "ui:full": "npm run ui:build && git add . && git commit -m uibuild && npm run ui:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.20.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.15",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  }
}
